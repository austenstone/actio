node_modules/
dist/
*.tsbuildinfo
coverage/
reports/
.stryker-tmp/
.DS_Store
*.log

# Generated by `actio build` when pins resolve to digests; not tracked (pins stay tag-form in source).
actio.lock

# Bundled action dist must be committed so GitHub can run it without a build step.
!.github/actions/**/dist/
!.github/actions/**/dist/**
