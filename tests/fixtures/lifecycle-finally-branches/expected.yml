# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Finally Branches
on:
  - push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ./deploy.sh
  notify-shipped:
    runs-on: ubuntu-latest
    steps:
      - run: ./notify.sh
    needs:
      - deploy
    if: success()
  page-oncall:
    runs-on: ubuntu-latest
    steps:
      - run: ./page.sh
    needs:
      - deploy
    if: failure()
