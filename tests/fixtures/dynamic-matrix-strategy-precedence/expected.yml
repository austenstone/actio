# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Dynamic Matrix Strategy Precedence
on:
  - push
jobs:
  actio_setup_inline_wins:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            echo '["x"]' | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  inline_wins:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        keep:
          - manual
      fail-fast: true
    steps:
      - run: echo ${{ matrix.keep }}
    needs:
      - actio_setup_inline_wins
    if: needs.actio_setup_inline_wins.outputs.matrix != '[]' && needs.actio_setup_inline_wins.outputs.matrix != ''
  actio_setup_defaults_overridden:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            echo '["y"]' | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  defaults_overridden:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ matrix.shard }}
    strategy:
      matrix:
        shard: ${{ fromJSON(needs.actio_setup_defaults_overridden.outputs.matrix) }}
      fail-fast: true
    needs:
      - actio_setup_defaults_overridden
    if: needs.actio_setup_defaults_overridden.outputs.matrix != '[]' && needs.actio_setup_defaults_overridden.outputs.matrix != ''
