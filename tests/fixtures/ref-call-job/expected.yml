# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Ref Call Job
on:
  - push
jobs:
  lib:
    uses: ./.github/workflows/lib.yml
  app:
    runs-on: ubuntu-latest
    steps:
      - run: echo "using ${{ needs.lib.outputs.version }}"
    needs:
      - lib
