# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: ci
on:
  - push
jobs:
  actio_setup_test:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            jq -cn '$ARGS.positional' --args core cli | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  test:
    runs-on: ubuntu-latest
    steps:
      - run: npm test --workspace ${{ matrix.pkg }}
    needs:
      - actio_setup_test
    strategy:
      matrix:
        pkg: ${{ fromJSON(needs.actio_setup_test.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_test.outputs.matrix != '[]' && needs.actio_setup_test.outputs.matrix != ''
  teardown:
    runs-on: ubuntu-latest
    needs:
      - test
    if: ${{ !cancelled() }}
    steps:
      - run: ./teardown.sh
  teardown-on-cancel:
    runs-on: ubuntu-latest
    needs:
      - test
    if: ${{ cancelled() }}
    timeout-minutes: 5
    steps:
      - run: ./teardown.sh
