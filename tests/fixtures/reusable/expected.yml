# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Reusable
on:
  push:
    branches:
      - main
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
        description: Target environment
      verbose:
        type: boolean
        default: false
    secrets:
      deploy-token:
        required: true
    outputs:
      url:
        description: Deployed URL
        value: ${{ jobs.deploy.outputs.url }}
  workflow_dispatch:
    inputs:
      environment:
        type: string
        required: true
        description: Target environment
      verbose:
        type: boolean
        default: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.run.outputs.url }}
    steps:
      - id: run
        env:
          INPUTS_KEPT: ${{ 'github.event.inputs.kept' }}
        run: |
          echo "deploying to ${{ inputs.environment }}"
          echo "verbose=${{ inputs.verbose }}"
          echo "snapshot=${{ toJSON(inputs) }}"
          echo "literal $INPUTS_KEPT"
