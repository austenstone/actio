# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Deploy
on:
  - push
jobs:
  plan:
    runs-on: ubuntu-latest
    outputs:
      cfg: ${{ steps.step_build_config.outputs.cfg }}
    steps:
      - uses: actions/checkout@v4
      - name: Build config
        id: step_build_config
        run: |-
          {
            echo 'cfg<<ACTIO_EOF_cfg_5a2ed6f6'
            ./scripts/plan.sh
            echo ACTIO_EOF_cfg_5a2ed6f6
          } >> "$GITHUB_OUTPUT"
  deploy:
    runs-on: ubuntu-latest
    needs:
      - plan
    steps:
      - run: ./deploy.sh ${{ fromJSON(needs.plan.outputs.cfg).env }} ${{ fromJSON(needs.plan.outputs.cfg).region }}
  notify:
    runs-on: ubuntu-latest
    needs:
      - plan
    steps:
      - run: echo "deployed to ${{ fromJSON(needs.plan.outputs.cfg).region }}"
