# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: json-fanout
on:
  - push
jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - name: collect meta
        id: step_collect_meta
        run: |-
          {
            echo 'info<<ACTIO_EOF_INFO_3BAHJ0'
            cat meta.json
            echo ACTIO_EOF_INFO_3BAHJ0
          } >> "$GITHUB_OUTPUT"
    outputs:
      info: ${{ steps.step_collect_meta.outputs.info }}
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploying ${{ fromJSON(needs.meta.outputs.info).version }} at ${{ fromJSON(needs.meta.outputs.info).sha }}"
    needs:
      - meta
