# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: JSON Fanout
on:
  - push
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: Plan
        id: step_plan
        run: |-
          {
            __ACTIO_EOF="ACTIO_EOF_cfg_$(openssl rand -hex 8 2>/dev/null || echo ${RANDOM}${RANDOM})"
            echo "cfg<<${__ACTIO_EOF}"
            ./scripts/plan.sh
            echo "${__ACTIO_EOF}"
          } >> "$GITHUB_OUTPUT"
    outputs:
      cfg: ${{ steps.step_plan.outputs.cfg }}
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: deploy --env ${{ fromJSON(needs.plan.outputs.cfg).env }}
    needs:
      - plan
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "region ${{ fromJSON(needs.plan.outputs.cfg).region }}"
    needs:
      - plan
