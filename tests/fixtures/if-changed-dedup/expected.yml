# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: If Changed Dedup
on:
  - push
  - pull_request
jobs:
  actio_changes:
    runs-on: ubuntu-latest
    outputs:
      filter_1: ${{ steps.actio_filter.outputs.filter_1 }}
      filter_2: ${{ steps.actio_filter.outputs.filter_2 }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: actio_filter
        with:
          filters: |-
            filter_1:
              - 'services/api/**'
            filter_2:
              - 'docs/**'
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ./deploy.sh
    if: needs.actio_changes.outputs.filter_1 == 'true'
    needs:
      - actio_changes
  smoke:
    runs-on: ubuntu-latest
    if: needs.actio_changes.outputs.filter_1 == 'true' && github.ref == 'refs/heads/main'
    steps:
      - run: ./smoke.sh
        if: needs.actio_changes.outputs.filter_2 == 'true'
    needs:
      - actio_changes
