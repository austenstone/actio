# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: ci
on:
  - push
jobs:
  actio_setup_build:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            echo '${{ fromJSON(needs.plan.outputs.shards) }}' | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./run-shard.sh ${{ matrix.shard }}
    needs:
      - actio_setup_build
    strategy:
      matrix:
        shard: ${{ fromJSON(needs.actio_setup_build.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_build.outputs.matrix != '[]' && needs.actio_setup_build.outputs.matrix != ''
