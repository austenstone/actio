# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Ref Cross Job
on:
  - push
jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - name: Compute
        run: echo "sha=$GITHUB_SHA" >> "$GITHUB_OUTPUT"
        id: step_compute
    outputs:
      sha: ${{ steps.step_compute.outputs.sha }}
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "first ${{ needs.meta.outputs.sha }}"
      - run: echo "second ${{ needs.meta.outputs.sha }}"
    needs:
      - meta
