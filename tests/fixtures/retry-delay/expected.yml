# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Retry Delay
on:
  - push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy (attempt 1/3)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_TOKEN }}
        id: step_deploy_attempt_1
        continue-on-error: true
      - name: Retry backoff (10s) before attempt 2/3
        run: sleep 10
        if: steps.step_deploy_attempt_1.outcome == 'failure'
      - name: Deploy (attempt 2/3)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_TOKEN }}
        id: step_deploy_attempt_2
        if: steps.step_deploy_attempt_1.outcome == 'failure'
        continue-on-error: true
      - name: Retry backoff (10s) before attempt 3/3
        run: sleep 10
        if: steps.step_deploy_attempt_2.outcome == 'failure'
      - name: Deploy (attempt 3/3)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_TOKEN }}
        id: step_deploy_attempt_3
        if: steps.step_deploy_attempt_2.outcome == 'failure'
