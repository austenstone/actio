name: CI
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ./deploy.sh

finally:
  teardown:
    runs-on: ubuntu-latest
    steps:
      - run: ./cleanup.sh
  on_abort:
    rollback:
      runs-on: ubuntu-latest
      steps:
        - run: ./rollback.sh
