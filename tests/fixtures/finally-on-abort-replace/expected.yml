# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: CI
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh
  test:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - run: ./test.sh
  teardown:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    if: ${{ !cancelled() }}
    steps:
      - run: ./stop-resources.sh
  teardown-cancelled:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    if: ${{ cancelled() }}
    timeout-minutes: 5
    steps:
      - run: ./stop-resources.sh
      - run: ./notify.sh "run cancelled"
