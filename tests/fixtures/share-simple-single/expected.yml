# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Release
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Resolve version
        run: |-
          VERSION=$(jq -r .version package.json)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        id: step_resolve_version
    outputs:
      version: ${{ steps.step_resolve_version.outputs.version }}
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: npm publish --tag v${{ needs.build.outputs.version }}
    needs:
      - build
