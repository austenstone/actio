# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Soft Fail
on:
  - push
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Tolerate known codes
        run: |-
          __actio_sf_file="$(mktemp)"
          printf '%s\n' './flaky-check.sh' > "$__actio_sf_file"
          __actio_sf_code=0
          bash --noprofile --norc -eo pipefail "$__actio_sf_file" || __actio_sf_code=$?
          rm -f "$__actio_sf_file"
          case "$__actio_sf_code" in 0|42) exit 0 ;; *) exit "$__actio_sf_code" ;; esac
        shell: bash
      - name: Tolerate any failure
        run: ./best-effort.sh
        continue-on-error: true
