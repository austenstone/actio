name: Deploy
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: ./deploy.sh
        on_failure:
          - run: ./rollback.sh
        on_success:
          - run: ./smoke-test.sh
