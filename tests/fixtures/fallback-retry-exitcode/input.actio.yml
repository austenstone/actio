name: Fallback Retry Exit Code
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        run: ./run-tests.sh
        fallback:
          retry:
            runs-on: ubuntu-latest-16-cores
            when-exit-code: [137]
