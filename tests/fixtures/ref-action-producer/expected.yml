# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Ref Action Producer
on:
  - push
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        id: ref_setup_step_1
    outputs:
      node-version: ${{ steps.ref_setup_step_1.outputs.node-version }}
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "on ${{ needs.setup.outputs.node-version }}"
    needs:
      - setup
