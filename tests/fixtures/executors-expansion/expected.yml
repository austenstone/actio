# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Executors expansion
on:
  - push
jobs:
  train:
    steps:
      - run: echo train
    runs-on:
      - self-hosted
      - gpu
    env:
      FROM_DEFAULT: "1"
      FROM_EXECUTOR: "1"
    container:
      image: node:20
      env:
        FROM_EXECUTOR: "1"
    services:
      redis:
        image: redis:7
  override:
    runs-on: ubuntu-24.04
    container:
      image: alpine:3.20
    services:
      postgres:
        image: postgres:16
    env:
      FROM_DEFAULT: "1"
      FROM_JOB: "1"
    steps:
      - run: echo override
