name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh

finally:
  teardown:
    runs-on: ubuntu-latest
    steps:
      - run: ./stop-resources.sh
  on_abort: []
