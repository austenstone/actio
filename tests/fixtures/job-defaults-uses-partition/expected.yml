# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Job defaults uses partition
on:
  - push
jobs:
  call-release:
    uses: org/repo/.github/workflows/release.yml@main
    if: github.ref == 'refs/heads/main' && success()
    permissions:
      contents: read
    strategy:
      matrix:
        node:
          - 20
    concurrency:
      group: default-group
      cancel-in-progress: true
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo test
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
    strategy:
      matrix:
        node:
          - 20
    concurrency:
      group: default-group
      cancel-in-progress: true
    timeout-minutes: 15
    env:
      CI: "true"
