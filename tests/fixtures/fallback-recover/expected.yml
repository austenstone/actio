# Code generated by Actio. DO NOT EDIT.
# Edit the .actio source and run `actio build` instead.
# Source: input.actio

name: Fallback Recover
on:
  - push
jobs:
  flaky:
    runs-on: ubuntu-latest
    steps:
      - name: Flaky step
        run: ./maybe-fails.sh
        id: step_flaky_step
        continue-on-error: true
      - name: Cleanup and continue
        run: ./cleanup.sh
        if: steps.step_flaky_step.outcome == 'failure'
