name: required
on: [push]
jobs:
  prep:
    runs-on: ubuntu-latest
    steps:
      - name: derive tag
        run: echo preparing
        share:
          tag:
            value: ${{ github.ref_name }}
            required: true
  use:
    runs-on: ubuntu-latest
    steps:
      - run: echo "tag is ${{ share.tag }}"
