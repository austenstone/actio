# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: required
on:
  - push
jobs:
  prep:
    runs-on: ubuntu-latest
    steps:
      - name: derive tag
        run: |-
          echo preparing
          echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          [ -n "${{ github.ref_name }}" ] || { echo "::error::empty share value"; exit 1; }
        id: step_derive_tag
    outputs:
      tag: ${{ steps.step_derive_tag.outputs.tag }}
  use:
    runs-on: ubuntu-latest
    steps:
      - run: echo "tag is ${{ needs.prep.outputs.tag }}"
    needs:
      - prep
