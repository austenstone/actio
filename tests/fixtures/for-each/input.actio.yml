name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - for-each:
          var: item
          in: [alpha, beta]
        steps:
          - run: echo {{ item }}
      - run: echo done
