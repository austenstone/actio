# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: multiline
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: gen notes
        id: step_gen_notes
        run: |-
          {
            echo 'notes<<ACTIO_EOF_NOTES_M6JNPF'
            echo line one
            echo line two
            echo ACTIO_EOF_NOTES_M6JNPF
          } >> "$GITHUB_OUTPUT"
    outputs:
      notes: ${{ steps.step_gen_notes.outputs.notes }}
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: |
          cat <<'MSG'
          ${{ needs.build.outputs.notes }}
          MSG
    needs:
      - build
