# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Multiline
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Notes
        id: step_notes
        run: |-
          {
            __ACTIO_EOF="ACTIO_EOF_notes_$(openssl rand -hex 8 2>/dev/null || echo ${RANDOM}${RANDOM})"
            echo "notes<<${__ACTIO_EOF}"
            git log --oneline -n 20
            echo "${__ACTIO_EOF}"
          } >> "$GITHUB_OUTPUT"
    outputs:
      notes: ${{ steps.step_notes.outputs.notes }}
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ needs.build.outputs.notes }}"
    needs:
      - build
