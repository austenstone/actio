# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Changelog
on:
  - push
jobs:
  changelog:
    runs-on: ubuntu-latest
    outputs:
      notes: ${{ steps.step_notes.outputs.notes }}
    steps:
      - uses: actions/checkout@v4
      - name: Notes
        id: step_notes
        run: |-
          {
            echo 'notes<<ACTIO_EOF_notes_e3edb7ea'
            git log --oneline -n 20
            echo ACTIO_EOF_notes_e3edb7ea
          } >> "$GITHUB_OUTPUT"
  release:
    runs-on: ubuntu-latest
    needs:
      - changelog
    steps:
      - uses: softprops/action-gh-release@v2
        with:
          body: ${{ needs.changelog.outputs.notes }}
