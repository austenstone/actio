name: When Compile Fixture Presence
on: [push]
params:
  deploy:
    type: boolean
    default: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo start
      - name: Publish preview
        static_if: params.deploy
        run: echo publish
      - run: echo end
