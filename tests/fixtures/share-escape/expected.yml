# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: Escape
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Doc
        run: |-
          TAG=$(date +%s)
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
        id: step_doc
      - run: echo "real ${{ steps.step_doc.outputs.tag }} literal ${{ share.tag }}"
