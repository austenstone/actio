# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: escape
on:
  - push
jobs:
  emit:
    runs-on: ubuntu-latest
    steps:
      - name: make color
        run: |-
          echo making
          echo "color=blue" >> "$GITHUB_OUTPUT"
        id: step_make_color
    outputs:
      color: ${{ steps.step_make_color.outputs.color }}
  show:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "literal ${{ share.color }}"
          echo "real ${{ needs.emit.outputs.color }}"
    needs:
      - emit
