# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: same-job
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: compute token
        run: |-
          echo computing
          echo "token=abc123" >> "$GITHUB_OUTPUT"
        id: step_compute_token
      - run: echo "token is ${{ steps.step_compute_token.outputs.token }}"
