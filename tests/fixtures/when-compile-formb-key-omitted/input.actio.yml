name: When Compile Form B Key Omitted
on: [push]
params:
  debug:
    type: boolean
    default: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          static-if(params.debug):
            retention-days: "1"
