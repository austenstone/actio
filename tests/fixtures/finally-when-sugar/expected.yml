# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: input.actio.yml

name: CI
on:
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh
  test:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - run: ./test.sh
  deploy:
    runs-on: ubuntu-latest
    needs:
      - test
    steps:
      - run: ./deploy.sh
  triage:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
      - deploy
    if: ${{ failure() && needs.deploy.result == 'failure' }}
    steps:
      - run: ./triage.sh ${{ needs.deploy.result }}
