# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: .github/actio/e2e/e2e-dynamic-matrix.actio.yml

name: E2E Dynamic Matrix
on:
  workflow_call: null
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  actio_setup_shards:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            echo '["alpha","beta","gamma"]' | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  shards:
    runs-on: ubuntu-latest
    steps:
      - name: Assert shard value
        run: |
          case "${{ matrix.shard }}" in
            alpha|beta|gamma) echo "ok shard: ${{ matrix.shard }}" ;;
            *) echo "unexpected shard: ${{ matrix.shard }}"; exit 1 ;;
          esac
    needs:
      - actio_setup_shards
    strategy:
      matrix:
        shard: ${{ fromJSON(needs.actio_setup_shards.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_shards.outputs.matrix != '[]' && needs.actio_setup_shards.outputs.matrix != ''
  collect:
    runs-on: ubuntu-latest
    needs:
      - shards
    if: always()
    steps:
      - name: Assert all shards passed
        run: |
          test "${{ needs.shards.result }}" = "success"
          echo "dynamic matrix verified: fan-out expanded and every shard ran green"
  actio-annotate:
    name: Actio annotate
    runs-on: ubuntu-latest
    needs:
      - actio_setup_shards
      - shards
      - collect
    if: failure()
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/actio-annotate
        with:
          token: ${{ github.token }}
