# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: .github/actio/e2e/e2e-fragments-executors.actio.yml

name: E2E Fragments + Executors
on:
  workflow_call: null
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  smoke:
    steps:
      - name: Fragment step
        run: echo "ACTIO_FRAGMENT=ran" >> "$GITHUB_ENV"
      - name: Assert fragment + executor applied
        run: |
          test "$ACTIO_FRAGMENT" = "ran"
          test "$ACTIO_EXECUTOR" = "applied"
          echo "structural sugar verified: job_defaults + executor + fragment applied"
    runs-on: ubuntu-latest
    env:
      ACTIO_EXECUTOR: applied
    timeout-minutes: 5
  actio-annotate:
    name: Actio annotate
    runs-on: ubuntu-latest
    needs:
      - smoke
    if: failure()
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/actio-annotate
        with:
          token: ${{ github.token }}
