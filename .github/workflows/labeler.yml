# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: .github/actio/labeler.actio.yml

name: Labeler
on:
  pull_request: null
permissions:
  contents: read
jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/labeler@v5
  actio-annotate:
    name: Actio annotate
    runs-on: ubuntu-latest
    needs:
      - label
    if: failure()
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: ./.github/actions/actio-annotate
        with:
          token: ${{ github.token }}
