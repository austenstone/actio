# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/ensure-job/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: docker compose up -d
      - run: ./run-tests.sh
      - run: docker compose logs > logs.txt
        if: always()
      - run: docker compose down
        if: always()
