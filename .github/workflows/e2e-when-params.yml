# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: .github/actio/e2e/e2e-when-params.actio.yml

name: E2E When + Params
on:
  workflow_call: null
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Assert param interpolation
        run: |
          test "hello-from-actio" = "hello-from-actio"
          echo "params verified: greeting interpolated at build time"
      - name: Kept by static_if
        run: echo "kept step ran (static_if true)"
  actio-annotate:
    name: Actio annotate
    runs-on: ubuntu-latest
    needs:
      - smoke
    if: failure()
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/actio-annotate
        with:
          token: ${{ github.token }}
