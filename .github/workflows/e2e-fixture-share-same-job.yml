# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/share-same-job/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Same Job
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Tag
        run: |-
          TAG=$(date +%s)
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
        id: step_tag
      - run: echo "building ${{ steps.step_tag.outputs.tag }}"
