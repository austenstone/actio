# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/dynamic-matrix/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Dynamic Matrix
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  actio_setup_test:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            ./scripts/list.sh | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  test:
    runs-on: ubuntu-latest
    steps:
      - run: ./run-shard.sh ${{ matrix.shard }}
    needs:
      - actio_setup_test
    strategy:
      matrix:
        shard: ${{ fromJSON(needs.actio_setup_test.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_test.outputs.matrix != '[]' &&
      needs.actio_setup_test.outputs.matrix != ''
