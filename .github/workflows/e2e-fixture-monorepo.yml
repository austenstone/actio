# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/monorepo/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Monorepo CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  actio_setup_test_changed_services:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            ./scripts/get-changed-dirs.sh | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  test_changed_services:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Run Tests
        run: |
          cd ${{ matrix.service }}
          npm install && npm test
        id: step_run_tests
      - name: Alert Slack on Failure
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: '{"text":"Tests failed for ${{ matrix.service }}"}'
        if: failure() && steps.step_run_tests.conclusion == 'failure'
    needs:
      - actio_setup_test_changed_services
    strategy:
      matrix:
        service: ${{ fromJSON(needs.actio_setup_test_changed_services.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_test_changed_services.outputs.matrix != '[]' &&
      needs.actio_setup_test_changed_services.outputs.matrix != ''
