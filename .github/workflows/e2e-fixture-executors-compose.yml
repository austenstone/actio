# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/executors-compose/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Executors compose
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  release:
    steps:
      - run: echo release
    permissions:
      contents: read
    concurrency:
      group: hardened-group
    defaults:
      run:
        shell: bash
    env:
      HARDENED: "true"
    timeout-minutes: 10
    runs-on:
      - self-hosted
      - gpu
    container:
      image: nvidia/cuda:12.4.0-base
  tuned:
    runs-on: windows-latest
    steps:
      - run: echo tuned
    permissions:
      contents: read
    concurrency:
      group: hardened-group
    defaults:
      run:
        shell: bash
    env:
      HARDENED: "true"
      FAST: "true"
    timeout-minutes: 5
