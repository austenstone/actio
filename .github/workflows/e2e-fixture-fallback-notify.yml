# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/fallback-notify/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Fallback Notify
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: ./deploy.sh
        id: step_deploy
      - name: Notify
        run: ./notify.sh failure
        if: failure() && steps.step_deploy.conclusion == 'failure'
