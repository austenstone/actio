# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/share-simple-single/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Release
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Resolve version
        run: |-
          VERSION=$(jq -r .version package.json)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
        id: step_resolve_version
    outputs:
      version: ${{ steps.step_resolve_version.outputs.version }}
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: npm publish --tag v${{ needs.build.outputs.version }}
    needs:
      - build
