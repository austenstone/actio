# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/everything/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Everything
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install (attempt 1/3)
        run: npm ci
        id: step_install_attempt_1
        continue-on-error: true
      - name: Retry backoff (5s) before attempt 2/3
        run: sleep 5
        if: steps.step_install_attempt_1.outcome == 'failure'
      - name: Install (attempt 2/3)
        run: npm ci
        id: step_install_attempt_2
        if: steps.step_install_attempt_1.outcome == 'failure'
        continue-on-error: true
      - name: Retry backoff (5s) before attempt 3/3
        run: sleep 5
        if: steps.step_install_attempt_2.outcome == 'failure'
      - name: Install (attempt 3/3)
        run: npm ci
        id: step_install_attempt_3
        if: steps.step_install_attempt_2.outcome == 'failure'
      - name: Lint
        run: npm run lint
        id: step_lint
        continue-on-error: true
      - run: echo "lint failed, continuing"
        if: steps.step_lint.outcome == 'failure'
  actio_setup_test:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          {
            echo 'matrix<<ACTIO_EOF'
            ./scripts/shards.sh | jq -c .
            echo ACTIO_EOF
          } >> "$GITHUB_OUTPUT"
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Test shard
        run: ./scripts/test.sh ${{ matrix.shard }}
      - name: Report failure
        run: ./scripts/report.sh
        if: failure()
    needs:
      - actio_setup_test
    strategy:
      matrix:
        shard: ${{ fromJSON(needs.actio_setup_test.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_test.outputs.matrix != '[]' &&
      needs.actio_setup_test.outputs.matrix != ''
