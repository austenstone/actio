# Code generated by Actio. DO NOT EDIT.
# Edit the .actio.yml source and run `actio build` instead.
# Source: .github/actio/e2e/e2e-fallback-recover.actio.yml

name: E2E Fallback Recover
on:
  workflow_call: null
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  recover:
    runs-on: ubuntu-latest
    steps:
      - name: Flaky
        run: exit 1
        id: step_flaky
        continue-on-error: true
      - name: Recover
        id: recover_step
        run: echo "recovered=yes" >> "$GITHUB_OUTPUT"
        if: steps.step_flaky.outcome == 'failure'
      - name: Assert recovery ran
        run: |
          test "${{ steps.recover_step.outputs.recovered }}" = "yes"
          echo "fallback recover verified: failing step recovered, job green"
  actio-annotate:
    name: Actio annotate
    runs-on: ubuntu-latest
    needs:
      - recover
    if: failure()
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/actio-annotate
        with:
          token: ${{ github.token }}
