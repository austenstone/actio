# TEMPORARY experiment (phase 2): VALID reusable workflow behind an if:false
# caller. Expected: driver run == success, this job SKIPPED (0 minutes).
name: zz-exp-callee
on:
  workflow_call:
jobs:
  a:
    runs-on: ubuntu-latest
    steps:
      - run: echo "callee body would run here (should NOT, due to if:false)"
