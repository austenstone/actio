# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/finally-outcome-branches/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ./deploy.sh
  notify-success:
    runs-on: ubuntu-latest
    needs:
      - deploy
    if: success()
    steps:
      - run: ./notify-ok.sh
  notify-failure:
    runs-on: ubuntu-latest
    needs:
      - deploy
    if: failure()
    steps:
      - run: ./page-oncall.sh
  notify-abort:
    runs-on: ubuntu-latest
    needs:
      - deploy
    if: cancelled()
    timeout-minutes: 5
    steps:
      - run: ./notify-cancelled.sh
