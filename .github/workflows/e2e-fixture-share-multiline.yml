# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/share-multiline/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Multiline
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Notes
        id: step_notes
        run: |-
          {
            echo 'notes<<ACTIO_EOF_notes_79f4fb'
            git log --oneline -n 20
            echo ACTIO_EOF_notes_79f4fb
          } >> "$GITHUB_OUTPUT"
    outputs:
      notes: ${{ steps.step_notes.outputs.notes }}
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ needs.build.outputs.notes }}"
    needs:
      - build
