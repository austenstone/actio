# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/job-defaults-normal-merge/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Job defaults normal merge
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    steps:
      - run: echo build
    env:
      CI: "true"
      LOG_LEVEL: debug
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        shell: bash
