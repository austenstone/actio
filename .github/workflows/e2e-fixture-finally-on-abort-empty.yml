# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/finally-on-abort-empty/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: ./deploy.sh
  teardown:
    runs-on: ubuntu-latest
    needs:
      - deploy
    if: "!cancelled()"
    steps:
      - run: ./cleanup.sh
