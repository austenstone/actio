# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/bare-finally/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh
  test:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - run: ./test.sh
  teardown:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    if: "!cancelled()"
    steps:
      - run: ./destroy-preview-env.sh
  teardown-on-cancel:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    if: cancelled()
    timeout-minutes: 5
    steps:
      - run: ./destroy-preview-env.sh
