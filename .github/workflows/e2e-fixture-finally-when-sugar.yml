# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/finally-when-sugar/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: CI
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: ./build.sh
  test:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - run: ./test.sh
  deploy:
    runs-on: ubuntu-latest
    needs:
      - test
    steps:
      - run: ./deploy.sh
  rollback:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
      - deploy
    if: failure() && needs.deploy.result == 'failure'
    steps:
      - run: ./rollback.sh
