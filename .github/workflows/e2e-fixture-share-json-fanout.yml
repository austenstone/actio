# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/share-json-fanout/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: JSON Fanout
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - name: Plan
        id: step_plan
        run: |-
          {
            echo 'cfg<<ACTIO_EOF_cfg_b7cac5'
            ./scripts/plan.sh
            echo ACTIO_EOF_cfg_b7cac5
          } >> "$GITHUB_OUTPUT"
    outputs:
      cfg: ${{ steps.step_plan.outputs.cfg }}
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: deploy --env ${{ fromJSON(needs.plan.outputs.cfg).env }}
    needs:
      - plan
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "region ${{ fromJSON(needs.plan.outputs.cfg).region }}"
    needs:
      - plan
