# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/fallback-recover/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Fallback Recover
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  flaky:
    runs-on: ubuntu-latest
    steps:
      - name: Flaky step
        run: ./maybe-fails.sh
        id: step_flaky_step
        continue-on-error: true
      - name: Cleanup and continue
        run: ./cleanup.sh
        if: steps.step_flaky_step.outcome == 'failure'
