# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/dynamic-matrix-pwsh/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: dynamic-matrix-pwsh
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  actio_setup_build:
    runs-on: windows-latest
    outputs:
      matrix: ${{ steps.actio_eval.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
      - name: Evaluate dynamic matrix
        id: actio_eval
        run: |-
          $actioOut = & {
            $names = Get-ChildItem -Directory packages | ForEach-Object { $_.Name }
            $names | ConvertTo-Json -Compress
          }
          $actioBody = ($actioOut | Out-String).TrimEnd()
          [System.IO.File]::AppendAllText($env:GITHUB_OUTPUT, "matrix<<ACTIO_EOF`n$actioBody`nACTIO_EOF`n", (New-Object System.Text.UTF8Encoding $false))
        shell: pwsh
  build:
    runs-on: windows-latest
    steps:
      - run: echo "building ${{ matrix.pkg }}"
    needs:
      - actio_setup_build
    strategy:
      matrix:
        pkg: ${{ fromJSON(needs.actio_setup_build.outputs.matrix) }}
      fail-fast: false
    if: needs.actio_setup_build.outputs.matrix != '[]' &&
      needs.actio_setup_build.outputs.matrix != ''
