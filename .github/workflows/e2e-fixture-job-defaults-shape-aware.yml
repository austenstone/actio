# Code generated by the Actio E2E harness. DO NOT EDIT.
# Source: tests/fixtures/job-defaults-shape-aware/input.actio.yml
# Regenerate: npm run build:e2e-fixtures

name: Job defaults shape aware
on:
  workflow_call: {}
  workflow_dispatch: {}
jobs:
  override-shapes:
    runs-on: ubuntu-latest
    permissions: write-all
    concurrency:
      group: custom
      cancel-in-progress: true
    steps:
      - run: echo one
  inherited:
    runs-on: ubuntu-latest
    steps:
      - run: echo two
    permissions:
      contents: read
    concurrency: ci-${{ github.ref }}
