{
  "name": "actio-annotate-action",
  "version": "0.1.0",
  "description": "Map failed GitHub Actions steps back to their .actio.yml source and emit annotations.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
